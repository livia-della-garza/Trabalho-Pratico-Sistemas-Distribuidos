[project]
name = "academic-assistant"
version = "0.1.0"
requires-python = ">=3.12"

[tool.ruff]
src = ["src"]
exclude = [".venv", "data", "docs"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.ruff.lint.per-file-ignores]
"src/services/frontend/app.py" = ["E501"]
